node_modules
dist
dev-dist
.vite
*.log

# Tauri (Rust) build artifacts
src-tauri/target/
src-tauri/gen/
# Cargo.lock IS committed for Tauri application crates (binary, not library) —
# locking transitive deps makes `tauri build` reproducible across contributors.

# Capacitor generated platform projects (require JDK/Xcode to regenerate; treat as build output)
android/
ios/
.capacitor/
