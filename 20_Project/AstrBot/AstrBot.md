---
type: project
status: active
area:
tags:
  - chatbot
  - self-hosted
gitignored: true
---
## AstrBot

A self-hosted chatbot platform (desktop app + web UI) for running conversational AI locally.

### Key Facts

- **What**: Desktop application with backend server and web UI
- **Stack**: Tauri (desktop), web-based frontend
- **Local folder**: `20_Project/AstrBot/` (gitignored — contains binaries)

### Why Gitignored

Contains large installer binaries (`.exe`) and runtime files unsuitable for git sync.
