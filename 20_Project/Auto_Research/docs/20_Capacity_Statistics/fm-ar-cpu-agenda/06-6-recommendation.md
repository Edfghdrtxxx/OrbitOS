<!-- Verbatim source section; overview: [[../fm-ar-cpu-agenda]] -->
<!-- SOURCE-BODY-START -->
## 6. Recommendation

1. **Dispatch Rank 1 (amendment package) and Rank 5 (closing-doc draft) immediately** — they are pure-documentation ships with the largest manuscript delta and zero compute.
2. **Dispatch Rank 2 + Rank 4 as one scout** — the paired-stats numbers are already computed above; the work is writing them up with per-channel/per-bin tables and manuscript-line mapping.
3. **Ask the lead for the B1 + B2 copies** (~250 MB total: 12 `predictions_paired.csv`, the s42 XA-Raw checkpoint, a 2000-event val-subset NPZ, `physics_feature_stats.json`). That single transfer converts the two highest-value pending prereg blocks (threshold_sweep + the locked-checkpoint conditions) into locally-scorable form — and this Mac then does the compute faster than box 176.
4. **Do not spend effort on:** Z01 re-downloads (numbers exist), stale-cache OOD claims beyond the established negative result, or any training substitute (none exists at ~100 h/run).

<!-- SOURCE-BODY-END -->
