<!-- Verbatim source section; overview: [[../fm-ar-closing-check]] -->
<!-- SOURCE-BODY-START -->
## Statement-by-statement table (ordered by how much each would mislead a reader)

| # | Doc §/line | Statement | Verdict | Evidence | Suggested replacement |
|---|---|---|---|---|---|
| R1 | §4 L41, L43, L45; also implicit in §1 table | "**test acc 0.92136**", "a valid completed-run **test** metric" | **contradicted (terminology) + unsupported (disclosure)** | referee A12; `metrics.json` has `best_val_acc: 0.9214`, `accuracy: 0.92136` — the split is 80/20 train/val (`data_split.json` `test_size: 0.2`); **no held-out test set exists anywhere in the campaign**. All reported metrics are val-set, and val doubles as early-stopping + best-checkpoint selection set. | "val acc 0.92136 (n=25k validation events — the same split used for early stopping and best-epoch selection; no held-out test set exists in this campaign)". Add one global sentence in §1: "All accuracies herein are validation-set metrics; selection bias is small but nonzero and arm-symmetric for paired Δs." |
| R2 | §5 L49 | "4He 2×2 (4 arms, seed 42): **0/4 complete.** XA-Raw-lf partial only." | **contradicted** | §4 of the same doc + §15 "label-fix run completed" + `runs/EXP3-XA-Raw-100k-label-fix-seed42/20260923_154749/metrics.json` (acc 0.92136, best_epoch 13, 22-epoch history). gpu-plan-draft §0, referee B.1, gpu-ladder §2.1 all say 1/4. | "4He 2×2 (4 arms, seed 42): **1/4 complete** — XA-Raw-lf done (0.92136); the remaining 3 arms never launched." |
| R3 | §12 L172 | "some seeds route meaningfully through query content (large zero_q cost), others barely use it" | **contradicted** | attn-sink §2: `permuted_q` Δ=0 on 6/6 checkpoints means **no** seed routes through query *content*. The zero_q cost measures how far the `query_proj` bias vector `b_q` sits from the trained realistic-query cone — a seed-dependent geometric accident, not content usage. | "the attention pathway's dependence on the *position* of the query vector is learned and unstable across seeds — zero_q cost varies −5.8 to −57.1pp — while no seed routes through query *content* (permuted_q Δ=0 on all 6 checkpoints)." |
| R4 | §8 L112 | "the model needs *a* varying query (or the query_proj bias is badly calibrated as a constant), but doesn't care which" | **contradicted** | attn-sink §2: `zero_q` feeds `physics=0` → `q = b_q`, a *fixed* point that can lie outside the dominant attention region. Variation per se is irrelevant; the model needs a query **inside the trained cone**. | "a constant bias query hurts (−6.5pp) — `b_q` lands outside the trained query cone and produces a different fixed attention pattern; the model needs a query inside that cone, not a varying one." |
| R5 | §8 L111, §9 L129, §12 L162 | "query content carries **zero** event-specific signal" (stated unqualified, "on BOTH representations", "anywhere") | **weakened** | (a) attn-sink §2 caveat + referee A3 + feature-norm §3c: on **Raw** the physics features are near-constant (total_mass CV≈3%, |r|≤0.038; Izz≈170 DC dominates the ~383-norm query), so permutation barely moves the query — Raw's Δ=0 is a weak measurement; the strong evidence is HC (CV≈27%) + corrected 4He. (b) referee A4: n=2000 → "Δ≤0.0005" bounds the effect at <~0.2% of predictions, not zero; the battery JSONs record `n_events` but **not which events**, so cross-checkpoint subset identity is unverifiable. | "permuted_q = original on all 6 checkpoints (Δ≤0.05pp at n=2000 — bounds any content effect to <~0.2% of predictions). Strong form proven on HC and 4He where the query genuinely varies; on Raw the query is nearly constant by construction (Izz≈170 DC dominates), so the same conclusion holds via a different mechanism: the map is image-driven." |
| R6 | §3 L37 | "On Raw, attention is **event-adaptive** and Bragg-focused — **routing works**; the deficit lives downstream of routing" | **weakened** (part unsupported) | attn-sink §3: f_Bragg=0.47 is consistent with an event-adaptive map *and* with a fixed spatial prior — argmax-dispersion was never recorded for Raw (`exp4_attention_metrics.py` persists no per-event argmax). Single seed (s42; XA-Raw-s0/lf never run — referee A10). And "routing works" invites the refuted reading that the physics query steers the map — the map is **key(image)-driven**, not query-driven. | "On Raw the attention map concentrates on the Bragg region (f_Bragg≈0.47 vs 0.20 null, seed 42 only) and is image-driven — the physics query demonstrably does not steer it (permuted_q Δ=0). Whether the Raw map is event-adaptive or a fixed spatial prior is unmeasured. The deficit lives downstream of the map." |
| R7 | §16 L200 | "the XA-Raw deficit is **not a model failure** — the input features are uninformative on Raw" | **contradicted as written** | The separability result shows the *physics features* are uninformative on Raw — but the deficit is XA vs RN **on identical images**. RN-Raw extracts 0.89 from the same input; XA's head/architecture fails to match it. §16's own evidence localizes the failure to the model's image pathway, it does not acquit the model. | "the XA-Raw deficit is not a *physics-feature* failure — the features are uninformative on Raw either way. The deficit is a failure of XA's image pathway (routing + 68-dim head) to extract the class signal RN's GAP head finds in the same images." |
| R8 | §10 L150 NOTE | "the classifier-physics **dependence** is **task-invariant/architectural**, not triton-specific" | **weakened** | referee A2, results-audit F1, hc-vs-raw §3 OOD caveat: `zero_cls` landing at minority prevalence (0.1935 ≈ 0.20) is the signature of an OOD single-class collapse; whether it reflects *dependence* or a *zeroing artifact* is unresolved until D5 (`permuted_cls`/`mean_cls` — still absent from all 6 synced JSONs, verified today). What is task-invariant is the collapse *signature*. | "the zero_cls collapse signature (→ minority-class prevalence) is task-invariant — it replicates on 4He (0.1935) exactly as on triton s42. Whether that reflects information dependence or an OOD bias artifact remains unresolved pending D5." |
| R9 | §2 L27 | "the strongest single piece of evidence for H2 (noisy direct physics) over pure H1 (static capacity): a pure width limit would plateau, not degrade" | **weakened** | referee A5: the recipe (lr 1e-4, dropout 0.3, wd 1e-4 — identical across all 12 configs, verified) was inherited, not tuned per-arm. A model peaking at ep7 and degrading is equally consistent with generic under-regularization/over-noising; no hyperparameter control exists. | "consistent with noisy physics features entering twice — and equally consistent with generic under-regularization on an untuned recipe. It rules out a *pure static* capacity limit but does not by itself select H2; a hyperparameter control or D1–D4 is needed." |
| R10 | §1 L17 | "The attended 64-dim vector + 4 noisy features under-serves the hard class" | **unsupported** (as mechanism) | referee A6/A7: (a) no class weights, default argmax threshold — balanced-accuracy gap survives (−2.7/−4.6pp) so it's not *purely* calibration, but a threshold sweep is unrun; (b) 68-dim head vs 512-dim GAP confounds "physics hurts" with "narrow head hurts"; (c) D1–D4 (which would localize this) have not landed. | "The deficit concentrates in class-0 recall (−4.9/−8.7pp; survives in balanced accuracy at −2.7/−4.6pp). Whether the cause is the narrow 68-dim head, the noisy physics concat, or calibration is unlocalized pending D1–D4 and the threshold sweep." |
| R11 | §14 L192 OPEN | "feature-normalization audit … if physics features were meant to be normalized but aren't, that's a pipeline bug, not a design flaw" | **contradicted (premise resolved)** | feature-norm §1: features are raw **by deliberate spec** — `dataset.py:472-475` ("NOT normalized, Req-7.2"), `normalization.py:24-27` ("explicitly NOT normalized… Published results used raw un-normalized physics features"), spec D-PHYS-NORM records the legacy runs did the same. Not a bug — but `main.tex:170,207` falsely claims standardization (methods-audit M1). | "RESOLVED: physics features are unnormalized by deliberate spec (Req-7.2; published legacy runs identical) — a design choice, not a pipeline bug. Separately, the manuscript's 'standardized using training-set statistics' sentence is false for both reproduction and legacy runs (methods-audit M1; captain's file)." |
| R12 | §13 L180 | "McNemar bound p≤0.035 s0, p≤7.5e-4 s42" | **still holds but superseded** | gpu-ladder §2.2 computed tighter worst-case bounds using actual error counts: **p≤1.5e-5 (s0), p≤4.5e-12 (s42)** — guaranteed under every discordant split. | Update to the tighter bounds: "McNemar-guaranteed p≤1.5e-5 (s0) / p≤4.5e-12 (s42) under every discordant split." |
| R13 | §15 L210 Pending | "corrected lf battery JSON" listed as pending | **contradicted (stale)** | Verified on disk: `runs/EXP3-XA-Raw-100k-label-fix-seed42/20260923_154749/counterfactual_battery.json` (mtime 15:21) **is the corrected run** — original 0.9275, zero_q 0.8065, permuted_q 0.9275, zero_cls 0.1935, zero_both 0.5235. The buggy 0.7005 file is gone. (NB: attn-sink §6, mechanism §4, seed-evidence §8, gpu-plan-draft Q1 all still flag the local JSON as buggy — those flags are stale; this report verifies the corrected file is present.) | Move to Done: "corrected lf battery JSON synced (0.9275 original); buggy 0.7005 file no longer on disk." |
| R14 | §7 L62-64 | "That instance is also off" (checkpoint-pull plan premised on box being down) | **contradicted (stale)** | §15 of the same doc: "Box 176: **up** in no-GPU mode" — and §8/§9/§11 batteries already ran there. The pull plan's premise is stale; the battery path it describes as hypothetical already executed. | "Box 176 is up in no-GPU mode (§15) — the battery already ran on-box CPU; the pull plan remains as the fallback for getting weights local." |
| R15 | §5 L52 | counterfactual battery "implemented, **cannot run locally** — needs ~5 min GPU or a checkpoint pull" | **contradicted (stale)** | §8/§9/§11 of the same doc: the battery ran on box 176 CPU (2GB cgroup) and 6/6 checkpoint JSONs are synced. | Remove from §5 pending list (it's done — §8/§9/§11); keep only the not-yet-run conditions (D5/D6) as pending. |
| R16 | §1 L7 | "the buggy label map (raw label 4 = triton as class 0)" | **weakened (imprecise)** | label-blast §1.1: the map `{4:0}` is **correct under the v6 convention** (`4He=4`) and silently wrong under the OLD convention (`t=4`) that the box-176 files carry. The bug is a map↔file-convention mismatch, not a wrong map — this distinction is exactly what decides whether V6 Table 4 is also buggy (§1.4, unsettled). | "the label map↔file-convention mismatch (`{4:0}` is correct for v6-mapped files where 4He=4, but the box-176 files are OLD-mapped where t=4 → triton-vs-rest)." |
| R17 | §15 L208 Done | "paired-stats tooling deployed (PR#1)" | **still holds, with a correctness caveat** | code-audit §3/§4: `paired_stats.align_pair` **silently drops shared events whose labels disagree** (warns, then computes on the reduced population) and **accepts duplicate event IDs** (keeps last). McNemar itself verified vs SciPy oracle. | Append: "caveat: align_pair silently drops label-mismatched events and keeps last-duplicate IDs (code-audit §3-4) — require zero-mismatch + unique IDs before quoting paired outputs." |
| R18 | §4 L41 | "early-stopped ep21, best val 0.9214 @ ep13" | **still holds (trivial naming)** | `history.json` has 22 entries (epochs 0–21); gpu-plan-draft says "early-stopped at epoch 22". Off-by-one epoch indexing only. | "early-stopped after 22 epochs (best val 0.9214 @ ep13)." |
| R19 | §9 L129 (2) | "a constant bias query produces a bad fixed attention pattern; magnitude is seed-dependent" | **still holds, refinable** | attn-sink §2 gives the mechanism: `b_q` lands outside the dominant-query cone → different fixed map; magnitude = cone distance (seed accident). | Optional refinement: "…— `b_q` falls outside the trained query cone by a seed-dependent distance." |
| R20 | §16 L196 | "³He–⁴He hardest pair (0.617)" | **still holds** | Consistent with difficulty report (³He–⁴He TV=0.0045 — models treat them nearly identically) and label-blast §2.4 (d→t dominant confusion on triton task). | — |
| R21 | §8 L113, §9 L129(3), §10 L149 | zero_cls "ambiguous … D5 decides"; "load-bearing" provisional | **still holds** | Correctly hedged; D5 conditions verified absent from all 6 synced battery JSONs today (5 conditions each). | — |
| R22 | §10 L150 CORRECTION | s0-anomaly signature **does** replicate on 4He (+33pp gap) | **still holds** | referee A11 (the correction source); verified in the on-disk corrected JSON (0.5235 vs 0.1935). | — |
| R23 | §11 L154 | first lf battery void (positional-label bug); "+9.2pp flip" retracted | **still holds** | Corrected JSON on disk confirms the retraction (zero_q −12.1pp). | — |
| R24 | §13 L176-178 | difficulty + comparisons summaries | **still holds** | Matches fm-ar-difficulty §4-5 and fm-ar-comparisons §1/§5 verbatim in substance. | — |
| R25 | §14 L186-190 | OOD-leak + E1 audit summary; "strengthens crowding branch of H1" | **still holds** | Matches fm-ar-ood-leakage §2/§5/§6 and feature-norm §6. The crowding reading is a hypothesis the doc presents as such. | — |
| R26 | §1 table, §2 table, §3 table, §8/§9/§12 tables | all numeric cells | **still holds** | results-audit §1 recomputed every cell; my spot-checks of the 6 battery JSONs + metrics.json agree. The earlier results-audit F5 nits (run count, "500 correct", s42-HC zero_cls sign) are all fixed in this revision. | — |

---

<!-- SOURCE-BODY-END -->
