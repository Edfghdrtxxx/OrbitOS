<!-- Verbatim source section; overview: [[../fm-ar-claims-refresh]] -->
<!-- SOURCE-BODY-START -->
## 1. Updated ledger — TIER 1 (wrong or void as written)

| # | Loc | Claim | Status @16:53 | Status now | New deciding evidence | Settling check (updated) |
|---|-----|-------|---------------|------------|----------------------|--------------------------|
| 1 | L369 | §5.5 "³He/⁴He task … 95.80/95.77 … +0.024 pp architecture" decomposition | wrong (task mislabeled) | **wrong — unchanged; fix now costed** | nimpsim-cost §2.5: Option B = ~20–35 GPU-h (range 10–50), does not fit the 9.5 h reserve; README's 4–6 h underived. | NimpSim Option B as first job of a future ~35 h session (`gpu_session_queue.md` §5); or Option A pair ~10 h. NimpSim H5 staging from IMP still unverified (`nimpsim_reserve/README.md` §staging). |
| 2 | L381 | "on the ³He/⁴He task the ordering was the same (96.98/96.94/95.80) … smaller validation set" | inflated ~1.2 pp + task mislabeled | **inflated — unchanged; settling check now packaged** | PR #23 shipped `scripts/analysis/exp2_clean_rescore.py`; PR #31 `windows_visit_checks.py` runs it as check [C] in one command on the Windows box. | `windows_visit_checks.py` on the author's Windows box — one JSON covering V6 labels + EXP2 rescore + label-blast verdict (`20_doc/audits/windows_visit_checks_runbook.md`). |
| 3 | L170, L207 | "physics features are normalized/standardized" | wrong | **wrong — unchanged; mechanism now measured by D6** | D6: `scaled_cls` collapses on Raw (0.4095 s0 / 0.1935 lf, closing doc §10b–10c) → the head reads the *scale* (Izz DC), confirming the raw-scale mechanism; on HC `scaled_cls` 0.954 ≈ original → pure static bias. | Text fix needs nothing. The `physics_norm=zscore` retrain (R3b) gate — "run only if `scaled_cls` improves the existing checkpoint" (prereg `gpu_runs.physics_norm_zscore`, yaml:394) — is **not met**: scaled_cls collapsed, did not improve. |
| 4 | L255 (Table 2 caption) | "V4 baselines report final-epoch values" | wrong | **wrong — unchanged** | — | None — spec-documented. |
| 5 | L248 | "V4 baselines use no label smoothing and gradient clipping 1.0" | wrong for 1 of 3 | **wrong — unchanged; + citation error on same line** | bib-fixsheet §2: `Jadon2020Loss` (segmentation-loss survey) is miscited for label smoothing → `Szegedy2016LabelSmoothing`. | None. |
| 6 | L526, L531 | "All runs share the AdamW optimizer … 5-epoch linear warmup" | wrong for 2 baselines | **wrong — unchanged; + citation error** | bib-fixsheet §2: `\cite{Adam}` (Kingma&Ba) cited for AdamW → `AdamW` (Loshchilov&Hutter, arXiv:1711.05101). | None. |
| 7 | L263 + L190 | Table 2 "Baseline ResNet-18" 91.9% vs modified backbone | inflated (undisclosed arch mismatch) | **inflated — unchanged; L190 citation fabricated** | bib-fixsheet §1a: `He2020ResNetSmall` (arXiv:2002.03544) is a math paper — fabricated; replace `\cite{ResNet,He2016Identity}` + reword. | Same as #1 (Option B, future session). |
| 8 | L298 | "XA outperforms RN by +0.8 pp HC / +1.6 pp Raw … most valuable when image quality is degraded" | inflated — contradicted at matched size | **inflated — unchanged; mechanism sharpened by D6** | D6: on Raw the head's physics dependence is magnitude-based static bias (permuted/mean ≈ orig, scaled collapses); on HC physics is pure static bias (scaled ≈ orig, zero_cls −0.1pp). The "+1.6 pp Raw" mechanism story is now contradicted by mechanism evidence, not just the −1.33/−2.13 pp matched-size result. | Rung 1 (R1 RN-Raw-lf s42) — still the decisive paired 4He comparator; queued as job 4 (`gpu_session_queue.md` §4, 14.5 billed h). Not yet run. |
| 9 | L313 + caption L319 | "elevated attention near track termination … physically consistent with isotope discrimination" | inflated (causal framing refuted) | **inflated — unchanged; + citation unverifiable + new D6 nuance** | bib-fixsheet §1c: `Koch2021` unverifiable → `NaturePhysicsReview` + `Jain2019Attention` ("attention is not explanation" caveat). D6 `centered_q`: −39pp (s0) / −6.8pp (lf) / −6.6pp (HC) — the query DC is load-bearing for routing even though query *content* is inert; refines but does not rescue the causal reading. figure-provenance RISK 1: pixels are real; framing is the problem. | Check A (map-level `permuted_q` on HC) still pending — it is also the prereg falsifier (`falsifiers.map_permuted_q_large_predictive`, yaml:423-439). |
| 10 | L53, L503 | "reaches 96.1% / 89.7% — gains of 4.2 and 6.9 pp" | inflated (numbers stand; gains unattributable) | **inflated — unchanged; + novelty sentence overstated** | related-work #1: L53/L67 "neither conventional cuts nor existing NN classifiers exploit the physics of energy loss" is overstated — Kuchera2019 (cited) uses physics-derived moment features; Dalitz 2018 (uncited) does AT-TPC trajectory recognition. Defensible claim = first *fusion* of physics descriptors with CNN features via attention. | Same as #1. |

<!-- SOURCE-BODY-END -->
