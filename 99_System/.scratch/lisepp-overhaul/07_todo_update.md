# 07 — TODO.md Update

**Date:** 2026-03-18
**File:** `D:\Something\research\LISE++\lisepp-mcp\TODO.md`

## What was done

Appended four new issue sections to TODO.md, preserving the two existing DONE entries:

| Issue | Status |
|---|---|
| `charge_double()` / `chargeSchiwietzGas()` return 0.0 | Known limitation (standalone DLL) |
| `global_code()` returns undocumented values | Known limitation (no DLL docs) |
| Batch mode incomplete | Open |
| Remaining test coverage gaps | Open |

The two resolved entries (material density conversion, numeric display precision) remain unchanged at the top of the file.
