# Evolution Log

## 2026-07-26
### Lessons
- Once active, present each turn in a `60_Learning_Progress/` note (not chat as primary): write the user's question + your reply. Reuse matching topic folder/note when it exists; only create `…/<topic>/<YYYY-MM-DD-HHmm> <subtopic>.md` if missing. Never parallel paths.
- **Read `60_Learning_Progress/Progress-context.md` first** every activation; update it tersely after turns that advance understanding (MEMORY-style index: solid / in progress / session notes).
- **All math in LaTeX** (`$...$` / `$$...$$`) in chat and progress notes — no plain-text formula stand-ins.

## 2026-03-14
### Lessons
- When spawning `/atomic-note` from the "Save as atomic note" option, detect whether the answered question is a comparison of 2+ concepts or a single concept. Use `/atomic-note extend TopicA, TopicB` for comparisons (→ `30_Research/<Area>/`), use `/atomic-note <topic>` for single concepts (→ `40_Wiki/`). Do not default everything to create mode.
